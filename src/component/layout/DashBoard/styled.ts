import styled from 'styled-components';

export const DashBoardStyled = styled.div`
    width: 80%;
    padding-top: 20px;
    position: absolute;
    .dashboard-ul {
        list-style-type: none;
        display: flex;
        overflow: hidden;
    }
    .menu-bar {
        float: left;
    }
    .content {
        padding-left: 50px;
        width: 100%;
    }
`;
